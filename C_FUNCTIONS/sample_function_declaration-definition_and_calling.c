//first we define our function

int add(int a, int b) {
    return a + b;
}

//then next we are going to use it, by calling it in our program.

//we will store the returned value in a variable then print it out

int main() {
    int result = add(3, 4);
    printf("%d", result);
    return 0;
}

