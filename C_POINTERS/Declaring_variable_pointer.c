//first we create an integer variable and name it x,then on the same line we assign it a value of 5
int x = 5;

//then we create an integer pointer then name the pointer as ptr
int *ptr;

//then we assign the address to the variable to our ponter as follows
ptr = &x;
